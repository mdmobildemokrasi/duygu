Bu sürüm AST1 Firebase projesine bağlanacak şekilde güncellendi.

Yapılanlar:
1) firebase-applet-config.json AST1 web config ile değiştirildi.
2) src/firebase.ts içinde özel firestoreDatabaseId kaldırıldı.
3) Firestore artık AST1 projesindeki (default) veritabanına bağlanıyor.

Kontrol listesi:
- Firebase Console'da proje: ast1-7b902
- Firestore database: (default)
- Firestore Rules publish edilmiş olmalı.
- AI Studio içinde sayfayı hard refresh ile yeniden yükleyin.

Beklenen sonuç:
- 'Database (default) not found' hatası kalkmalı.
- Rules doğruysa 'Missing or insufficient permissions' hatası da kalkmalı.
